function num(str){
    var newStr = "";
    for(var i=0;i<str.length;i++){
        if(!isNaN(str[i])){
            newStr += str[i]*2;
        }else{
            newStr += str[i];
        }
    }
    return newStr;
}
exports.num = num;